# 2026-09-21

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- TUI went unclickable after idle; timer refresh still painted. Event poll thread used to `break` on `poll`/`read` Err (sleep/EINTR), then `rx.recv()` returned None and starved input. Mouse-move also forced a full redraw per event. Retry poll errors, respawn the reader, skip move-only events, Skip missed ticks.
- Do not re-send EnableMouseCapture every tick — that ate clicks. Footer keys are now mouse hits; second click on the selected row opens detail.
- Grok error was expired CLI JWT (~6h). Grok CLI refreshes via OIDC `refresh_token` at `{issuer}/oauth2/token` and rotates the RT. Adapter now does the same and writes `auth.json` back.
