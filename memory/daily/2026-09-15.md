# 2026-09-15

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- Adopted repo-quality: replaced QUALITY.md stub, added `.githooks/check-quality.sh`, `.qualityignore`, `quality/` control files.
- `quality/` is not in agent-kit `kit_dir`; nested `quality/AGENTS.md` is required before files there.
- Landed approved remaining-quota TUI design under `docs/design/` split by concern (QUALITY `doc` class is 600 lines / 32KB; the monolith was ~1341 lines).
