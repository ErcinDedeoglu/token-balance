# 2026-09-19

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- Grok card HTTP 401 with a live JWT: `from_account` cached `key`; timer refresh does not `reload_live`. Re-read `~/.grok/auth.json` on each fetch. JWT `exp` ~6h; `grok login` rewrites the file. Same JWT HTTP 200 via urllib/curl/reqwest native-tls. 401 message includes `grok login`.
- Exa `GET dashboard.exa.ai/api/get-credits`: Python urllib 200 + `orbCreditsInCents`; reqwest (native-tls and rustls) and curl 429 Vercel HTML checkpoint. Not a dead cookie. Keep 180s backoff; do not open Chrome from `tb`.
- Exa remaining fetch switched to wreq 0.15.3 + wreq-util 0.1.0, Emulation::Chrome131, http1_only. Live probe HTTP 200 credits_json. wreq 0.16.1 needs rustc 1.98. cmake required (brew). CI/release install cmake.
- Muse vendor `muse` (Model API) HTTP 429 on manual `r` (burns Everyday). `muse-web` is the dashboard card and is not listed in this machine's accounts.toml.
- Muse 429 body `error.resets_at` maps to Everyday 5h 0% remaining, not a raw HTTP error. No live POST in that change.
