# 2026-09-17

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- Per-card refresh: timer uses each provider's interval; errors set retry_at via error_backoff (rate-limit 180s, timeout 15s, else 30s). in_flight_ids so one slow card does not skip others.
- Claude extra-only usage (null 5h/7d, extra_usage enabled) maps to monthly Other("mo") plus extra credits; credentials may be an unofficial http(s) usage URL.
- Muse web RefreshPolicy::Interval(180s); GraphQL errors take the first message (80 chars). Extra credits line shows left and used/limit when a limit is present.
- Ignore `.opencode/opencode-loop/` (local session files).
- OSS: CI on ubuntu/macOS/windows; tag `vX.Y.Z` cuts GitHub release archives for five targets; `tooling/install/install.sh` and `install.ps1` download latest. Asset name `token-balance-<rustc-target>.tar.gz|.zip`. `.gitignore` `dist/` so pack into `target/pack/`.
- `install.sh --print-target` and `tag-matches-version.sh` are the exec-able mapping/version checks; `install_target.rs` tests drive those scripts plus `rustc_target`.
- Windows GHA `bash` is WSL with no distro; tests must call `C:\Program Files\Git\bin\bash.exe`.
- macos-13 Intel runners stayed queued; build `x86_64-apple-darwin` on macos-latest with `--target`.
- `tar -a -cf *.zip` on windows-latest produced a GNU tar; pack with Compress-Archive instead.
- README `curl | sh` runs dash on Debian; install.sh is POSIX (`set -eu`, `[`, no `local`). Tests exec `/bin/dash` or `sh`, not bash.
- Git for Windows `dash.exe` rejects `set -eu\\r`; `.gitattributes` `*.sh text eol=lf`.
- Exa is PrepaidWallet; `GET .../api-keys/{id}/usage` `total_cost_usd` is spend. Mapper Errors spend-only and `NO_MORE_CREDITS`. No live remaining GET.
- Exa remaining is unofficial `GET https://dashboard.exa.ai/api/get-credits` with next-auth session cookie. `orbCreditsInCents/100` USD. Search API key cannot. Bare curl 429 without browser User-Agent.
- Vercel 429 on get-credits from rustls/reqwest default; native-tls + Chrome UA + http1; 429 is rate-limit not cookie. Interval 180s.
- CLI curl/reqwest get-credits is Vercel checkpoint HTML 429; Chrome in-page GET 200. Exa fetch prefers chrome-mcp http_request when a dashboard.exa.ai tab exists.
- Firecrawl remaining is official GET /v2/team/credit-usage remainingCredits (+ planCredits). Historical totalCredits is spend. Credits unit, not USD.


