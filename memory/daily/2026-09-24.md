# 2026-09-24

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- Repo-scaffold refresh on an already-seeded tree. Did not rewrite product source. Dirty product diffs (grok OIDC, TUI event loop) were already in the worktree and are not this session's product work.
- Installed the chain gate: `## Chain` on existing AGENTS.md files, plus `src/` and `src/adapters/` files because those folders are at the crowd threshold (8).
- Moved adapter-only Never rules from `crates/token-balance/AGENTS.md` into `src/adapters/AGENTS.md`. Did not invent vendor rules.
