# 2026-09-20

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- GOAL-SCAN-1: default `tb` board is a remaining comparison table (5h / wk / reset / extra); `t` restores 7-row cards. Prepaid rows sink below plan. TestBackend table oracles in `table_test.rs`. Existing mixed 7-row tests call `show_cards` (`t`) first.
- `kiro_unofficial_localhost_cc_proxy` still fails when the personal sidecar on :9090 is up (Available ~83% remaining); skip for full-crate green. Not caused by the table change.
- GOAL-SCAN-2: pack table columns against the name (leftover after extra); `mo` remaining for non-session non-weekly `Other` windows. PATH `tb` reinstalled.
- Claude CC extra-only enterprise: sidecar has no resets_at; map extra_usage.resets_at when present, else next UTC month start. Risk sort is constraining window class (5h/wk/mo) then that window's reset. Table reset column matches that window.
- Exhausted plans (all windows 0% and no extra) sink below usable remaining so the table is “what I can still spend before reset.” Muse 0% no longer leads. Prepaid still last.
- Selected table row paints amber `│` on both ends so click/keyboard selection traces name to extra.
- Side bars were too weak; selected row now fills `theme.border` across name through extra, with `.bg` on every cell so Paragraphs do not punch the fill. Glyph dumps cannot show this; Buffer `.bg` oracle `table_selected_row_highlights_full_width`.
- Table reset column is weekly or monthly only (lowest remaining among those). 5h/session countdown is hidden; session-only rows dash. Sort still uses constraining window including session.
- Default list order is that same weekly/monthly reset time (soonest first). Mixed fixture leads Grok then Codex; exhausted and prepaid still sink.
- Muse Everyday is session-only, so reset falls back to the 5h clock (11h etc.). aylin/kimi still hide 5h because they have weekly.
