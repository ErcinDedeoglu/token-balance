# 2026-09-18

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- Grok SuperGrok billing GET `cli-chat-proxy.grok.com/v1/billing?format=credits`: same `~/.grok/auth.json` JWT is HTTP 200 via curl/urllib and HTTP 401 via default reqwest/rustls. Fetch with native-tls, HTTP/1, User-Agent `grok-shell`, headers `Authorization: Bearer` + `X-XAI-Token-Auth: xai-grok-cli`.
