# 2026-09-16

Append-only raw log. Do not load into AGENTS.md. Distill into `lessons/` only after verification.

- Codex live fetch now speaks app-server JSON-RPC: initialize, initialized, then account/rateLimits/read matched by id. Tests drive a fake stdio peer that returns Not initialized before handshake.
