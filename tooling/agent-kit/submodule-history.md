<!--
HISTORY.md — {{SUBMODULE_NAME}}. Agent-readable.
OWNS: lessons mined from this repo's PAST (commits, tags, releases) that are
useful for ANY future change. Task-neutral by construction.
Not here: current facts (NOTES.md) — if it is still true today it is a fact,
not history.
Update trigger: an explicit history dig ONLY. Never as a side effect of
routine work: a repo's past does not change because we touched it today.
-->

# HISTORY — {{SUBMODULE_NAME}}

## Last dug

`unreviewed` — the submodule sha this history was mined at. A dig that does not
update this line did not happen.

## Lessons

_Task-neutral only. Never frame an entry around the feature that triggered the
dig; anything whose value depends on today's work belongs in a daily note._
