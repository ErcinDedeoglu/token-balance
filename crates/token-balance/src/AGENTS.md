# crates/token-balance/src/

Binary crate modules. `main.rs` declares them; there is no `lib.rs`.

## Chain

- Up: `../AGENTS.md`
- Down: `adapters/AGENTS.md`

## Delta

- Vendor HTTP clients live in `adapters/`, not beside TUI, domain, or accounts modules.
