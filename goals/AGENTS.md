# goals/

Goal contracts for this repo. Not product source.

- Write `GOAL-*.md` from the goal-file template; run `python3 /Users/ercin/.grok/skills/goal-file/scripts/check-goal.py` on the file until exit 0.
- Do not implement a goal unless the user asked to execute.
- Do not put secrets, tokens, or `.env` values in a goal file.
